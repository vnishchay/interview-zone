class Auth {
    constructor() {
        this.authenticated = False ; 
    }
    login(cb) {
          // check if user is logged in ? 
          // how ? simple. check the token, check its validity, if 
          // valid then store set authenticated = true 
          // else set it to false. 
          // better to create a context for authentication 
          // 
    }
    logout(cb) {

    }
    isAuthenticated(cb){

    }
}